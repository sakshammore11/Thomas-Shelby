{
  "framework": "create-react-app",
  "buildCommand": "npm ci --prefix frontend || npm install --prefix frontend && npm run build --prefix frontend",
  "outputDirectory": "frontend/build",
  "functions": {
    "api/*.js": {
      "runtime": "nodejs20.x"
    }
  },
  "env": {
    "GEMINI_API_KEY": "@gemini_api_key"
  }
}
